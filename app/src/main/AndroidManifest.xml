<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vieir.projetocmu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".calendar.Calendar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register.UserRegister" />
        <activity android:name=".register.Login" />
        <activity android:name=".interests.UserInterests" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CriarEvento" />
        <activity
            android:name=".HorasL"
            android:label="@string/title_activity_lista_horas"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>